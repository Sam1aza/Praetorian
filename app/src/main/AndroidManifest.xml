<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sisifus.praetorian">
    <!-- Dando permissões de aceesso ao android -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logotipov3"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logotipov3"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Listview"></activity>
        <activity android:name=".Acitivity.ApontamentoActivity" />
        <activity
            android:name=".Acitivity.PainelInicial"
            android:label="@string/title_activity_painel_inicial"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Acitivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Acitivity.CadastroUsuarioActivity" />
    </application>

</manifest>